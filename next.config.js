/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '',
  output: 'export',
  // images: {
  //   unoptimized: false
  // },
  // future: { webpack5: true }
}

module.exports = nextConfig