"use client";

export default function PageTabs({
    names,
    onTabClicked
}: {
    names: string[];
    onTabClicked: (index: number) => void;
}) {

    return (
        <>
            
        </>
    );
}